package uk.ac.ebi.gxa.loader.service;

import org.junit.Test;

/**
 */
public class TestNcdfConversion {
    @Test
    public void testExportExperiment() {
        // Get an experiment from DB/NetCDF
        // run update on it - not it'll rewrite the file
        // make sure all the DB fields are shown in the NetCDF and IDF files properly
        // release the experiment
        // import the experiment
        // make sure all the fields are still the same
    }
}
