These scripts deploy an instance of Atlas to an already-running server.

In order to install a shiny new server, check out https://github.com/gxa/gxa/wiki/Installation

In order to create a data release bundle, refer to the `release` directory